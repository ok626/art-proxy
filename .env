# TMDB API key — https://www.themoviedb.org/settings/api
TMDB_API_KEY=your_tmdb_api_key_here

# Fanart.tv API key — https://fanart.tv/get-an-api-key/
FANART_API_KEY=your_fanart_api_key_here

# Port
PORT=3000

# TMDB image sizes (used only for TMDB fallback)
# Poster options:   w92, w154, w185, w342, w500, w780, original
# Backdrop options: w300, w780, w1280, original
POSTER_SIZE=w780
BACKDROP_SIZE=original

# Minimum likes on Fanart for an image to be considered (default: 5)
FANART_MIN_LIKES=2

# Cache TTLs in seconds

# How long to cache the pool of candidate URLs per title (default: 6 hours)
POOL_CACHE_TTL=21600

# How long the chosen poster is locked in per title (default: 24 hours)
# Set to 0 to always pick a new random poster from the pool on each request
POSTER_SELECTION_TTL=86400

# How long the chosen backdrop is locked in per title (default: 0 = always random)
BACKDROP_SELECTION_TTL=0

# Minimum Fanart pool size before TMDB is pulled in to supplement (default: 8)
FANART_MIN_POOL_SIZE=8
